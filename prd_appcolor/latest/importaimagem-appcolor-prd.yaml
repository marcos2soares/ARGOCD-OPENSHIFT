apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: appcolor
  namespace: openshift
spec:
  lookupPolicy:
    local: true
  tags:
  - name: green
    from:
      kind: DockerImage
      name: image-registry.openshift-image-registry.svc:5000/appcolor/appcolor:green
    importPolicy: {}
    referencePolicy:
      type: Source
